/* ─── Homepage problem section — copy ───────────────────────
 * Reframed from "most alerts go uninvestigated" (the industry-wide
 * failure) to "most alerts get resolved autonomously" (what OmniSense
 * does about it) — the team only touches the ones that need judgment. */
export const problem = {
  eyebrow: 'How it works',
  stat: 'Thousands of alerts a day. Your team only touches what matters.',
  body: 'OmniSense triages, investigates, and closes the routine alerts on its own. Every autonomous action passes through the governance gate you define, and analysts step in only when a case needs a human call.',
  legendLit: 'Resolved autonomously',
  legendDim: 'Escalated to your team',
} as const
