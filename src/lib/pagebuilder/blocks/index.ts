/** Side-effecting registration — importing this module populates the block registry. */
// hero-section and stats-section are disabled: they targeted the pre-redesign
// homepage components (HeroSection now reads from @/content/home/hero, and
// StatsSection no longer exists).
import './ctaSection'
import './faqAccordion'
import './featuresSection'
import './integrationsSection'
import './finalCtaSection'
